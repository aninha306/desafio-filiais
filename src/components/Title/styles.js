import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 40,
    color: "#afcdea",
    marginTop: 30, 
    fontFamily: "Cochin",
    textAlign: "center"
  },
});

export default styles;